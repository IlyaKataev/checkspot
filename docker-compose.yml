services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: checkspot
      POSTGRES_PASSWORD: checkspot
      POSTGRES_DB: checkspot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U checkspot"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
